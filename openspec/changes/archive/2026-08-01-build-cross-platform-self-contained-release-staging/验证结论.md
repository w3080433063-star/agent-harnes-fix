# 跨平台安装产物验证结论

- 验证日期：2026-07-30
- Change：`build-cross-platform-self-contained-release-staging`
- 构建宿主：macOS arm64
- 当前结论：共享Payload与平台薄打包实现完成；macOS双架构DMG通过；Windows双架构MSI等待真实Windows宿主

## 自动门禁

- `npm run check`：通过
- TypeScript/Vitest：79个测试文件通过、1个跳过；500项测试通过、4项跳过
- `tests/release`：7个文件、28项测试通过
- Rust Workspace fmt、Clippy和测试：通过
- Launcher：8个单元测试和3个CLI集成测试通过
- OpenSpec strict validation：通过

## 实现边界

- 构建基础设施统一位于`scripts/release/`，测试位于`tests/release/`，不再是npm Runtime Package；
- `packages/release-staging`、Project Reference和lockfile Workspace条目已删除；
- 固定十三文件Payload包含生产Desktop Controller、三Agent自安装Renderer IIFE、注册Pi/Claude Code的Host Bundle和Claude SDK许可证，并保留Node官方归档校验；
- Payload不再生成`release-manifest.json`、`SHA256SUMS.txt`或目录备份事务；
- macOS由系统`codesign`和`hdiutil`生成ad-hoc签名App及DMG，Release不再输出App ZIP；
- Windows由固定WiX Toolset 4.0.6和声明式WXS生成per-user MSI；
- GitHub Release Workflow覆盖四目标，只上传最终DMG/MSI与SHA旁车。

## 目标结果

| 目标 | Payload | 安装artifact | 仓库外smoke | Runtime支持状态 |
| --- | --- | --- | --- | --- |
| `macos-arm64` | 通过 | DMG创建、校验、签名和SHA验证通过 | App、生产Controller、三Agent Renderer、Claude Host闭包和私有Node通过 | `in-progress`，三Agent菜单、Pi/Claude路由与reload仍在真实复测 |
| `macos-x64` | 通过 | DMG创建、校验、签名和SHA验证通过 | Node通过Rosetta执行真实JS与Host Bundle | `not-run`，无原生Intel Desktop Gate |
| `windows-x64` | 未运行 | WiX实现和静态契约测试通过，MSI未生成 | 未运行 | `not-run`，缺少Windows宿主 |
| `windows-arm64` | 未运行 | WiX实现和静态契约测试通过，MSI未生成 | 未运行 | `not-run`，缺少Windows宿主和原生ARM验证环境 |

## macOS产物事实

- arm64产物：`build/release/0.0.0/macos-arm64/codexhost-0.0.0-macos-arm64.dmg`；
- arm64 SHA-256：`64cb1ff2e6b7f2aa150b9fbcd0110f4bb389777197fda765fafd6b6a2aa05280`；
- x64产物：`build/release/0.0.0/macos-x64/codexhost-0.0.0-macos-x64.dmg`；
- x64 SHA-256：`3a9092f5a4fdc6972d6aaa43570f542b9217e3e21f044ecc3cea8aa7e0771b7b`；
- 两份DMG均通过`hdiutil verify`，旧App ZIP与其SHA旁车已由构建命令清理；
- arm64 DMG只读挂载后，根目录包含`codexhost.app`和指向`/Applications`的链接；
- 两个App内Launcher、Shim和Node均为目标对应的单一Mach-O架构；
- 两个App都通过`codesign --verify --deep --strict`，签名类型为ad-hoc，无Team ID；
- 签名后的Node均执行真实V8脚本并返回`v24.13.1`；
- Host Bundle均运行到预期的`CODEXHOST_STOCK_CODEX_PATH is required`环境边界；
- 每个Payload严格包含十三个文件，`app/host-runtime.mjs`包含Claude Adapter/Agent SDK但不包含可选平台Claude Code包；三份Claude SDK许可证、Controller和生产Renderer均进入App，最终只有DMG拥有SHA-256旁车。

## 未关闭边界

本机没有`.NET`、PowerShell、WiX和Windows Installer环境，不能把WXS静态检查表述为MSI构建通过。需要在真实Windows宿主分别运行：

```text
npm run release:package -- --target windows-x64
npm run release:package -- --target windows-arm64
```

还需安装并启动生成的MSI，验证开始菜单无参数入口、卸载、私有Node、Launcher资源解析和真实Codex Desktop Gate。Release Workflow已承载这两个构建目标，但在它实际成功前不得宣称Windows安装包支持已经通过。
