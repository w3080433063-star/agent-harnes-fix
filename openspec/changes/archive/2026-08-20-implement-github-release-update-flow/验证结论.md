# GitHub Release 自更新链路验证结论

日期：2026-08-07

## 当前结论

GitHub Release 发现、严格更新 RPC、安装上下文解析、后台 Helper 启动、受管 Desktop 退出、重启后状态恢复和 Renderer Updates 页面已完成代码与聚焦自动化验证。真实旧版到新版升级 Gate 尚未完成，因此本 change 不能归档，也不能将 npm、Windows 或 macOS 目标标记为真实升级已验证。

## 已执行证据

- `npm run build`：通过；包含 TypeScript、Renderer browser bundle、Launcher、Shim、Platform 和 Updater 构建。
- `npm run typecheck`：通过。
- `npm run test:typescript`：通过，118 个测试文件通过、1 个跳过；863 项测试通过、5 项跳过。
- 本 change 涉及文件的 Prettier、ESLint 和模块边界检查：通过。
- `cargo clippy --locked --package codexhost-launcher --package codexhost-updater --all-targets -- -D warnings`：通过。
- `cargo test --locked --package codexhost-launcher --package codexhost-updater`：通过；Launcher 21 项、Updater 4 项。
- Host release bundle、Renderer production bundle构建：通过。
- `npx openspec validate implement-github-release-update-flow --strict`：通过。
- `git diff --check`：通过。
- 未认证访问 `https://api.github.com/repos/BytePioneer-AI/codex-host/releases/latest`：返回 HTTP 404；当前没有可用于真实升级 Gate 的 latest 正式 Release。

## 全仓检查的外部失败

`npm run check` 尚未整体通过。与本 change 无关的并行工作区改动仍有以下问题：

- Prettier：`renderer-draft-prewarm-runtime.ts`、`renderer-draft-prewarm-policy.test.ts`、`renderer-model-picker.ts`。
- ESLint：`renderer-usage-control.ts` 存在非空断言。
- 全 Workspace Rust Clippy：`crates/platform/src/lib.rs` 的测试导入 `node_entrypoint_path` 未使用。

这些文件未因本 change 被回退或改写。

## 待执行真实 Gate

- npm：从已发布旧版本升级到新版本，验证精确版本安装、Launcher退出、自动重启、成功反馈和安装失败恢复。
- macOS arm64：验证DMG下载与digest、ad-hoc签名检查、App Bundle替换、回滚、重启和目录不可写失败。
- Windows x64、Windows ARM64、macOS x64：在对应原生目标机执行同等旧版到新版升级、重启和失败恢复验证。
- 发布前确认公开latest API返回目标正式Release，且四个安装资产的名称、大小和`sha256:` digest完整可用。
