# Agent Harness Desktop · 桌面使用指南

## 启动

首次运行 `npm ci --ignore-scripts` 和 `npm run desktop:prepare`，然后双击 `Start-Desktop.cmd`，或执行 `npm start -- --no-build`。

启动会重启官方 Codex Desktop。此版本扩展桌面客户端，不需要打开旧 MVP 的本地网页。

## 能力对比

1. 进入桌面设置中的 **Harness 能力对比**。
2. 刷新已启用插件目录，按名称或 ID 搜索。
3. 点击 **检测原生能力** 查看状态、模型数量、权限、Fork、回退和子任务能力。
4. 点击 **导出能力报告** 保存检查结果。

检测可能启动原生进程，但不提交模型任务。检测耗时不是模型首字速度。失败或超时与“不支持”分开显示，未声明的能力不会被推断为可用。

报告只保留插件 ID、版本、状态、耗时、模型数量和能力字段；排除账号、工作目录、模型名称与原始错误。

## 安装与登录

各 Harness 使用自己的原生安装、认证和配置。先确认它能在本机独立运行，再在桌面工作台中使用。发现插件不等于引擎安装完成或模型访问已获授权。

## 更新

拉取本分支后重新运行 `npm run desktop:prepare`，再启动桌面版。上游官方安装包不包含本分支新增功能。

## 验证范围

此前已通过 66 项相关测试、类型检查、定向 ESLint 和界面构建；本机 Pi 原生能力检查曾成功。完整桌面注入及全部 Harness 的端到端交互尚未完成实机验收。

开发核查见 [基线记录](docs/desktop-baseline-audit.md)，来源见 [开源致谢](ACKNOWLEDGEMENTS.md)。
